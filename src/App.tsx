import './styles/global.css'

function App() {
  return (
    <main className="app-container">
      <section className="app-card">
        <h1>Bem-vindo ao novo projeto React</h1>
        <p>Este é um app básico usando as dependências atuais do package.json.</p>
        <div className="tips">
          <p>Edite <code>src/App.tsx</code> para começar.</p>
          <p>Execute <code>npm run dev</code> para iniciar o ambiente de desenvolvimento.</p>
        </div>
      </section>
    </main>
  )
}

export default App;
